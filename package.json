{
  "name": "bugger-ckpt",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:client && npm run setup:server",
    "setup:client": "cd bugger-ckpt.client && npm i",
    "setup:server": "cd bugger-ckpt.server && npm i",
    "start": "node bugger-ckpt.server/index.js"
  },
  "engines": {
    "node": "^14.15.4"
  }
}
